# Mỗi 8h hàng ngày chạy cron job để craw data mới nhất từ các trang báo 
# Call AI service để summary
# Gửi thông tin qua discord cho user


# Frontend
## Chức năng đăng nhập bằng gg với firebase 
    - Xử dụng react query, zustand, axios để quản lí auth 
## Cho phép user nhập webhook url (sẽ dùng để nhập message) (có khả năng mở rông nếu sau này dùng cái khác discord)